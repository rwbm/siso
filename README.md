# Simple ISO

Simple ISO is a lightweight implementation of a library to handle financial messages in ISO8583 format. This library is inspipred in the work made the [jPOS](https://jpos.org/) team, which is the standard for the Java.