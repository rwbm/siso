# Simple ISO

Simple ISO is a lightweight implementation of a library to handle financial messages in ISO8583 format. This work is inspipred in the work made the [jPOS](https://jpos.org/) comunity, which is the standard for the Java; but with a very simple approach.